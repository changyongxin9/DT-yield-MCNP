# Output Matching Audit

本次目标集合包含 49 份完成输出：旧探测器厚度 10、长度 14、距离 8、最终计数 15，以及 D50H75 输出 2。

- 47 份旧探测器输出具有对应输入，并通过输入回显、几何、NPS、F8、E8、GEB 和结束标志核对。
- D5075 输入仍缺失；A5075 输入已由用户恢复，但输入卡 NPS=5E7，而对应 output 实际 NPS=1E8，因此仍不作为严格 input-output 配对。
- 当前目标集合中没有归属无法确认的 outp。

已知文字问题：

- 旧探测器 `T04_H25_D01/NPS1E8/Disk_D10` 输出标题残留 `5E8`，但输入回显、实际 NPS 和其余模型内容均确认它是 1E8。
- D50H75 `A5075` 输出标题写 `5E7`，实际输入回显和 NPS 为 1E8。

所有 49 份原始输出包含超算 XSDIR 账户路径，不能直接公开。项目已经为全部 49 份生成 `_public.outp` 脱敏副本，详见 `output_manifest.csv`。
