# parameter.csv 核对

- 根历史 `parameter.csv` 含 T01-T10 共 10 行，记录 Rin=5 cm、H=25 cm、D=1 cm、Rout=6-15 cm，与厚度输入一致。
- `长度优化/parameter.csv` 含 14 行，T4/T5 两支线与 H=20-50 cm 输入一致。
- `距离优化/parameter.csv` 含 8 行，T4/T5 两支线与 D=1-4 cm 输入一致。
- 厚度目录本身没有第二份 `parameter.csv`，其参数表是历史根目录文件；项目按原样复制到 `01_旧探测器模拟总数据/parameter.csv`。

未发现需要修改原始参数表的数值不一致。
